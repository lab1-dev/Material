import {Component} from "@lab1/core";
import {SortableColumnProps} from "./SortableColumnProps";

export class SortableColumn<T,ModelType> extends Component implements SortableColumnProps<T, ModelType>{

}
